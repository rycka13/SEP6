export class Person {
  id: number;
  name: string;
  birth: number;

  public constructor(id: number, name: string, birth: number) {
    this.id = id;
    this.name = name;
    this.birth = birth;
  }
}
